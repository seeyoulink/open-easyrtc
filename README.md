# SeeYouLink / Open-EasyRTC

## Changes

1. Modify ```getPeerConnectionByUserId``` to return whole peerConn (easyrtc private) object instead just native pc object. 

2. Modify ```getRoomList``` to return empty array instead actual list.